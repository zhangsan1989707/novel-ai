用户让我继续分析。我需要查看当前项目的更多实现细节，包括：
1. 当前的Agent系统实现
2. 提示词系统
3. 扫榜和拆文功能
4. 知识库系统

然后我可以对比参考项目提出改进建议。

让我继续查看一些关键文档和参考项目的内容。我需要更详细地了解：
1. 当前项目的优化建议文档
2. 参考项目的 skills 结构

让我先看看优化建议文档，然后获取参考项目的更多细节。

The user is asking me to continue the analysis. I was looking at both projects to compare them and provide improvement suggestions. Let me fetch more details about the oh-story-claudecode project to better understand its architecture and features.

The user wants me to continue analyzing both projects and provide improvement suggestions. I've gathered a lot of information about both projects:

**Novel AI (current project)**:
- Next.js-based full-stack application
- 5 Agent system: Planner, Writer, Polisher, Validator, Summarizer
- Multi-AI provider support (OpenAI, Anthropic, Alibaba, DeepSeek, MiniMax, Volcano)
- Features: Chapter generation, character management, plotline tracking, layered summaries
- Web interface with React components
- Database: PostgreSQL with Prisma

**oh-story-claudecode (reference project)**:
- Claude Code skill package
- More specialized skill-based approach
- 7 Agents: story-architect, character-designer, narrative-writer, consistency-checker, story-researcher, reviewer, cover-generator
- Comprehensive knowledge base with 110+ writing techniques
- Automated hooks for workflow management
- File-based project structure for long novels
- Market scanning capabilities (起点, 番茄, 晋江)
- No web interface - command-line based

Key differences and improvement opportunities for Novel AI:

1. **Missing features in Novel AI**:
   - Market scanning (扫榜)
   - Cover generation
   - Multi-angle review system
   - Automated hooks
   - Knowledge base with writing techniques
   - Short story support
   - More sophisticated story research capability

2. **Architecture improvements**:
   - Story-researcher agent for external research
   - Better separation of concerns in agents
   - Quality review agent system
   - Hooks system for workflow automation

3. **Agent system improvements**:
   - More specialized agents (cover, research, review)
   - Better prompt engineering with reference materials
   - Model tier optimization (Opus/Sonnet/Haiku)

4. **Workflow improvements**:
   - Daily writing workflow
   - Outline locking mechanism
   - Market integration
   - Comprehensive writing technique library

I should write a comprehensive analysis and suggestions document in Chinese since that's the user's language preference.
现在我已经收集了足够的信息，让我为你提供详细的对比分析和改进建议报告。

---

# Novel AI vs oh-story-claudecode 对比分析报告

> 分析日期：2026-05-16

---

## 一、项目定位对比

| 维度         | Novel AI                 | oh-story-claudecode             |
| ------------ | ------------------------ | ------------------------------- |
| **产品形态** | Web 全栈应用             | Claude Code Skill 包            |
| **交互方式** | 可视化界面 + API         | 命令行对话式                    |
| **目标用户** | 网文作者（需要界面辅助） | 开发者/高级写作者（习惯命令行） |
| **核心能力** | 章节生成 + 项目管理      | 全流程创作 + 市场分析           |
| **部署方式** | Docker / 自托管          | 直接安装到 Claude Code          |

---

## 二、功能对比矩阵

### 2.1 创作流程覆盖

| 流程阶段 | Novel AI         | oh-story-claudecode         | 差距分析            |
| -------- | ---------------- | --------------------------- | ------------------- |
| 选题方向 | ❌                | ✅ 扫榜选材                  | **Novel AI 缺失**   |
| 拆文学习 | ⚠️ 基础分析       | ✅ 黄金三章/爽点/节奏        | **oh-story 更深入** |
| 大纲搭建 | ⚠️ 基础章节列表   | ✅ 卷纲+细纲+伏笔规划        | **Novel AI 可增强** |
| 正文写作 | ✅ 5 Agent 流水线 | ✅ narrative-writer          | 两者相当            |
| 去 AI 味 | ⚠️ 提示词层面     | ✅ 独立 deslop skill         | **oh-story 更系统** |
| 质量审查 | ⚠️ Validator      | ✅ story-review 4 Agent 对抗 | **Novel AI 可增强** |
| 封面生成 | ❌                | ✅ GPT-Image-2               | **Novel AI 缺失**   |
| 市场分析 | ❌                | ✅ 起点/番茄/晋江趋势        | **Novel AI 缺失**   |
| 资料研究 | ❌                | ✅ story-researcher          | **Novel AI 缺失**   |

### 2.2 Agent 系统对比

| Agent    | Novel AI           | oh-story-claudecode   |
| -------- | ------------------ | --------------------- |
| 架构设计 | story-architect    | ✅ 5 个专业 Agent      |
| 角色设计 | character-designer | ⚠️ 集成在 Writer       |
| 叙事写作 | writer             | ✅ narrative-writer    |
| 润色     | polisher           | ⚠️ 集成在 writer       |
| 校验     | validator          | ⚠️ consistency-checker |
| 摘要     | summarizer         | ❌ 无独立 agent        |
| 审查     | ❌                  | ✅ reviewer (4 Agent)  |
| 研究     | ❌                  | ✅ story-researcher    |
| 封面     | ❌                  | ✅ cover-generator     |
| 浏览器   | ❌                  | ✅ browser-cdp         |

---

## 三、Novel AI 可新增功能清单

### 3.1 高优先级功能

#### 3.1.1 📊 扫榜选材模块 (`/story-long-scan`)

**功能描述**：自动抓取主流网文平台（起点、番茄、晋江）的榜单数据，分析市场趋势和热门题材。

**实现方案**：
```typescript
// API 路由设计
// /api/market/scan/ranking
// /api/market/analyze/trend
// /api/market/compare/platforms
```

**参考 oh-story 的能力**：
- 起点中文网榜单采集
- 番茄小说排行榜
- 晋江文学城数据
- 题材趋势分析
- 读者画像生成

#### 3.1.2 🔍 资料研究 Agent (`story-researcher`)

**功能描述**：在写作过程中自动搜索和整合外部资料（历史背景、专业知识、地理信息等）。

**当前差距**：Novel AI 没有这个能力，写作中遇到需要查证的外部事实时无法自动处理。

**实现方案**：
```typescript
// 新增 Agent
interface ResearchAgent {
  research(topic: string, context: WritingContext): Promise<ResearchResult>
}

// 输出结构
interface ResearchResult {
  topic: string
  summary: string        // 1-2 段总结
  sources: Source[]       // 3-5 个来源
  keyFacts: string[]     // 关键事实点
  confidence: 'high' | 'medium' | 'low'
}
```

#### 3.1.3 🖼️ 封面生成功能 (`story-cover`)

**功能描述**：基于小说题材和风格自动生成封面图。

**参考 oh-story 实现**：
- 书名和题材分析
- 调用 GPT-Image-2 生成封面
- 支持多种风格模板

**实现建议**：
```typescript
// /api/novel/cover/generate
interface CoverGenerationRequest {
  title: string
  genre: string
  style: 'xianxia' | 'urban' | 'scifi' | 'romance' | 'fantasy'
  colorScheme?: string[]
}
```

### 3.2 中优先级功能

#### 3.2.1 🛡️ 对抗式审稿系统 (`story-review`)

**功能描述**：4 Agent 从不同角度审查稿件，对标平台评分标准。

**参考 oh-story 的 4 Agent 审稿**：
| Agent      | 视角          | 评分维度           |
| ---------- | ------------- | ------------------ |
| 男频审稿人 | 起点/番茄标准 | 爽点密度、升级节奏 |
| 女频审稿人 | 晋江/知乎标准 | 感情线、人物塑造   |
| 毒点检测器 | 风险排查      | 敏感内容、政策红线 |
| 结构分析师 | 叙事结构      | 节奏、伏笔、逻辑   |

**实现价值**：
- 提高稿件整体质量
- 自动识别潜在问题
- 对标平台签约标准

#### 3.2.2 📚 写作技法知识库

**功能描述**：建立结构化的网文写作技法知识库，供 Agent 按需调用。

**参考 oh-story 的 110+ 技法库**：

| 类别       | 技法数量 | 说明                    |
| ---------- | -------- | ----------------------- |
| 大纲排布   | 8 种     | 五步大纲法、节点设计法  |
| 开头设计   | 12 种    | 黄金三章、前 500 字设计 |
| 人物设计   | 15 种    | 角色设定、动机链、群像  |
| 钩子技法   | 20+ 种   | 章尾 13 式、章首 7 式   |
| 情绪设计   | 10 种    | 6 种弧形模板            |
| 题材框架   | 15 种    | 长篇八节点、短篇三幕    |
| 对话技法   | 8 种     | 节奏、潜台词、信息控制  |
| 反转工具箱 | 12 种    | 类型、时机、误导路径    |
| 风格模块   | 10 种    | 打斗、智斗、装逼打脸    |
| 去 AI 味   | 30+ 规则 | 禁用词、改写范例        |

**实现方案**：
```
src/lib/knowledge/
├── hooks/
│   ├── chapter-end.md      # 章尾钩子 13 式
│   ├── chapter-start.md    # 章首钩子 7 式
│   └── paragraph.md        # 段落级钩子
├── style/
│   ├── combat.md           # 打斗写法
│   ├── dialogue.md         # 对话技法
│   └── emotion.md          # 情绪描写
├── anti-ai/
│   ├── forbidden-words.md   # 禁用词表
│   ├── patterns.md         # 改写范例
│   └── checklist.md        # 去 AI 味检查清单
└── formulas/
    ├── genre-basics.md     # 21 大题材公式
    └── emotional-arc.md    # 情绪弧线模板
```

#### 3.2.3 🔄 自动化工作流 Hooks

**功能描述**：在关键操作节点自动执行特定任务。

**参考 oh-story 的 5 个 hooks**：

| Hook                  | 触发时机     | 功能                   |
| --------------------- | ------------ | ---------------------- |
| session-start         | 会话开始     | 显示分支、进度快照     |
| detect-story-gaps     | 会话开始     | 检测设定缺口、大纲缺失 |
| pre-compact           | 上下文压缩前 | 保存进度快照           |
| post-compact          | 上下文压缩后 | 提示恢复上下文         |
| validate-story-commit | git commit   | 检查设定冲突           |

**Novel AI 可实现**：
```typescript
// hooks/commit-validation.ts
interface CommitValidationHook {
  trigger: 'pre-commit'
  action: (changes: FileChange[]) => ValidationResult
}

// 检查项
const validationRules = [
  '角色属性无硬编码',
  '设定字段必填',
  '伏笔有始有终',
  '时间线无矛盾'
]
```

### 3.3 低优先级功能

#### 3.3.1 短篇小说支持

**功能描述**：支持短篇网文创作，与长篇分开独立流程。

**参考 oh-story**：
- `/story-short-write` - 短篇写作
- `/story-short-analyze` - 短篇拆文
- `/story-short-scan` - 短篇扫榜

**短篇 vs 长篇差异**：
| 维度 | 长篇           | 短篇           |
| ---- | -------------- | -------------- |
| 字数 | 50-500 章      | 1-3 万字       |
| 结构 | 卷+章+节       | 三幕/起承转合  |
| 节奏 | 铺垫→爆发→回收 | 快节奏、强反转 |
| 情绪 | 渐进弧线       | 情绪爆发       |

#### 3.3.2 发布平台对接

**功能描述**：一键发布到各大网文平台。

**参考 oh-story 投稿指南**：
- 起点中文网 API
- 番茄小说后台
- 晋江文学城
- 七猫小说

---

## 四、代码架构改进建议

### 4.1 Agent 系统重构

**当前问题**：
- Agent 类型硬编码
- 缺乏统一的 Agent 接口
- 各 Agent 耦合度高

**改进方案**：

```typescript
// 定义 Agent 接口
interface Agent<TInput, TOutput> {
  readonly type: AgentType
  readonly name: string
  readonly description: string
  readonly model: ModelTier  // 模型层级

  execute(input: TInput, context: AgentContext): Promise<TOutput>
  validate?(output: TOutput): ValidationResult
}

// Agent 注册表
const agentRegistry = new Map<AgentType, Agent<any, any>>()

// 模型层级
enum ModelTier {
  OPUS = 'opus',    // 复杂推理、架构设计
  SONNET = 'sonnet', // 常规写作、角色设计
  HAIKU = 'haiku'    // 轻量检查、一致性验证
}

// Agent 映射（参考 oh-story）
agentRegistry.set(AgentType.ARCHITECT, { model: ModelTier.OPUS })
agentRegistry.set(AgentType.CHARACTER_DESIGNER, { model: ModelTier.SONNET })
agentRegistry.set(AgentType.NARRATIVE_WRITER, { model: ModelTier.SONNET })
agentRegistry.set(AgentType.CONSISTENCY_CHECKER, { model: ModelTier.HAIKU })
agentRegistry.set(AgentType.RESEARCHER, { model: ModelTier.SONNET })
```

### 4.2 提示词模块化增强

**当前结构**：
```
src/lib/prompts/
├── chapter/
│   ├── planning.ts
│   ├── writing.ts
│   ├── polishing.ts
│   ├── validating.ts
│   └── summarizing.ts
├── novel/
│   ├── idea.ts
│   ├── outline.ts
│   ├── synopsis.ts
│   └── ...
└── shared/
    └── constants.ts
```

**建议新增**：

```
src/lib/prompts/
├── knowledge/                    # 写作技法知识库
│   ├── hooks/
│   │   ├── chapter-end.ts       # 章尾钩子 13 式
│   │   ├── chapter-start.ts     # 章首钩子 7 式
│   │   └── suspense.ts          # 悬念设计
│   ├── style/
│   │   ├── combat.ts            # 打斗写法模板
│   │   ├── dialogue.ts          # 对话技法
│   │   └── description.ts       # 描写技法
│   ├── anti-ai/
│   │   ├── forbidden-words.ts   # 禁用词表
│   │   └── patterns.ts          # AI 味模式
│   └── formulas/
│       ├── genre-basics.ts      # 题材公式
│       └── emotional-arc.ts     # 情绪弧线
├── references/                   # 参考资料
│   ├── genre-handbook.ts        # 题材手册
│   ├── platform-guide.ts        # 平台投稿指南
│   └── reader-persona.ts         # 读者画像
└── templates/
    ├── outline-template.ts       # 大纲模板
    └── character-profile.ts     # 角色卡模板
```

### 4.3 工作流引擎增强

**新增功能**：

```typescript
// /api/workflow/daily
interface DailyWorkflowRequest {
  projectId: number
  targetChapters: number  // 今日目标章节数
  priority: 'speed' | 'quality'
}

// 工作流状态
interface WorkflowState {
  phase: 'outline' | 'writing' | 'review' | 'revision'
  progress: number
  remaining: string[]
  issues: Issue[]
}

// 断点续传
interface WorkflowCheckpoint {
  chapterNo: number
  outline: ChapterOutline
  draft: string
  issues: Issue[]
}
```

---

## 五、数据库模型扩展

### 5.1 新增模型建议

```prisma
// 市场数据采集
model MarketTrend {
  id            String    @id @default(cuid())
  platform      String    // 平台: qidian/tomato/jinjiang
  genre         String    // 题材
  rankDate      DateTime  // 榜单日期
  bookCount     Int       // 入榜书数
  avgWordCount  Int       // 平均字数
  updateFreq    String    // 更新频率

  createdAt     DateTime  @default(now())
  @@index([platform, genre, rankDate])
}

// 写作技法库
model WritingTechnique {
  id          String    @id @default(cuid())
  category    String    // 类别: hooks/style/emotion
  name        String    // 技法名
  description String    @db.Text
  examples    Json      // 正确/错误示例
 适用场景    String[]
  tags        String[]

  @@index([category])
}

// 封面模板
model CoverTemplate {
  id          String    @id @default(cuid())
  genre       String    // 适用题材
  style       String    // 风格
  colorScheme String[]  // 配色方案
  elements    Json      // 画面元素模板
  prompt      String    @db.Text  // 生成提示词模板

  @@index([genre])
}
```

### 5.2 现有模型增强

```prisma
// NovelProject 新增字段
model NovelProject {
  // ... 现有字段
  
  // 市场定位
  targetPlatform String[]   // 投稿平台
  competitorBooks String[]   // 对标书籍
  
  // 质量目标
  minSpPerChapter Int      // 每章最低爽点数
  targetUpdateFreq String   // 目标更新频率
  
  // 引用关系
  researchRefs    ResearchRef[]
}

// 研究资料引用
model ResearchRef {
  id          String    @id @default(cuid())
  projectId   Int
  project     NovelProject @relation(...)
  
  topic       String    // 研究主题
  summary     String    @db.Text
  sources     Json      // 来源列表
  usedInChapter Int?    // 使用的章节号
  
  createdAt   DateTime  @default(now())
}
```

---

## 六、实施优先级建议

### Phase 1: 核心能力补齐（2-3 周）

| 功能           | 工作量 | 优先级 | 说明           |
| -------------- | ------ | ------ | -------------- |
| 资料研究 Agent | 中     | 🔴 高   | 提升写作准确性 |
| 扫榜选材模块   | 大     | 🔴 高   | 选题阶段辅助   |
| 写作技法知识库 | 中     | 🔴 高   | 提升生成质量   |

### Phase 2: 质量提升（2-3 周）

| 功能           | 工作量 | 优先级 | 说明           |
| -------------- | ------ | ------ | -------------- |
| 对抗式审稿     | 中     | 🟡 中   | 多视角质量把控 |
| 去 AI 味系统化 | 小     | 🟡 中   | 提升稿件自然度 |
| 封面生成       | 中     | 🟡 中   | 完稿发布配套   |

### Phase 3: 高级功能（3-4 周）

| 功能         | 工作量 | 优先级 | 说明       |
| ------------ | ------ | ------ | ---------- |
| 短篇支持     | 大     | 🟢 低   | 扩展用户群 |
| 自动化 Hooks | 中     | 🟢 低   | 提升效率   |
| 平台对接     | 大     | 🟢 低   | 一键发布   |

---

## 七、关键技术借鉴

### 7.1 oh-story 的提示词工程

**借鉴点 1：铁律 vs 参考规则**
```typescript
// oh-story 的规则分级
const rules = {
  // 硬约束（必须遵守）
  hardRules: [
    '字数 ≥3000字/章',
    '前30章大纲确定后不得修改',
    '禁用词命中即替换'
  ],
  // 软规则（优先遵守）
  softRules: [
    '爽点密度 1/3000字',
    '章尾钩子必设'
  ],
  // 参考（可选使用）
  references: [
    '情绪弧线模板',
    '反转工具箱'
  ]
}
```

**借鉴点 2：模型分层调用**
```typescript
// 不同任务使用不同模型
const modelStrategy = {
  // 高推理需求
  highReasoning: { model: 'claude-opus', cost: 'high' },
  // 常规写作
  writing: { model: 'claude-sonnet', cost: 'medium' },
  // 轻量检查
  checking: { model: 'claude-haiku', cost: 'low' }
}
```

### 7.2 项目结构设计

**借鉴 oh-story 的文件组织**：
```
{书名}/
├── 设定/
│   ├── 世界观/
│   ├── 角色/
│   ├── 势力/
│   └── 题材定位.md
├── 大纲/
│   ├── 大纲.md
│   ├── 卷纲_第X卷.md
│   └── 细纲_第XXX章.md
├── 正文/
│   └── 第XXX章_章名.md
├── 对标/
│   └── {对标书名}/
│       ├── 原文/
│       └── 拆文报告.md
├── 追踪/
│   ├── 伏笔.md
│   ├── 时间线.md
│   └── 上下文.md
└── 参考资料/
    └── {topic}.md
```

---

## 八、总结

### 8.1 Novel AI 优势

| 优势         | 说明                     |
| ------------ | ------------------------ |
| 🎨 可视化界面 | 用户友好，适合非技术用户 |
| 📊 项目管理   | 完整的项目管理功能       |
| 🔗 数据库集成 | 结构化数据存储           |
| ⚡ 实时生成   | SSE 流式输出             |

### 8.2 需要追赶的能力

| 能力       | 重要性 | 建议     |
| ---------- | ------ | -------- |
| 扫榜选材   | 🔴 高   | 优先实现 |
| 资料研究   | 🔴 高   | 优先实现 |
| 写作技法库 | 🔴 高   | 优先实现 |
| 对抗式审稿 | 🟡 中   | 次优先   |
| 封面生成   | 🟡 中   | 次优先   |
| 短篇支持   | 🟢 低   | 后期考虑 |

### 8.3 核心建议

1. **短期（1-2 月）**：优先补齐「扫榜选材」和「资料研究」两个核心缺失功能，完善写作技法知识库
2. **中期（3-4 月）**：实现「对抗式审稿」和质量提升系统，加强去 AI 味能力
3. **长期（5-6 月）**：扩展短篇支持、自动化工作流、平台对接等高级功能

---

需要我针对某个具体功能提供更详细的实现方案吗？